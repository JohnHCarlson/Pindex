import React from 'react';

import Spinner from 'react-bootstrap/Spinner';

const Search = () => {

    return (
        <div>
            <h1>SEARCH</h1>
        </div>
    );
};

export default Search;